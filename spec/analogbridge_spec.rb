require "spec_helper"

describe Analogbridge do
  it "has a version number" do
    expect(Analogbridge::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
